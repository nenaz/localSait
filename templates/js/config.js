(function (app) {
    if (app) {
        app.setConfig({
            startNavPage : 1,
            catalogPath : 'D:\\Films\\',
            pictureWidth : 221,
            pictureHeight : 306,
        });
    };
})(App);