(function (app) {
    if (app) {
        app.setConfig({
            startNavPage : 1,
            catalogPath : 'D:\\Films\\',
            pictureWidth : 219,
            pictureHeight : 306,
            previewWidth : 445,
            previewHeight : 630,
            mainMenuWidth: 60
        });
    };
})(App);